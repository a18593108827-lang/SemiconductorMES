# 2026-09-18

- 浏览 MES 项目文档：架构总册、实施进度、Carrier C2 清单、Agent 数据暴露规划
- 建立项目长期记忆 MEMORY.md（项目定位/技术栈/模块边界/进度/文档约定）
- 注意：本机 Bash 环境 PATH 异常（ls/mkdir 等不可用），文件操作需用 Read/Write/Glob 工具
- 完成 AI 集成产品分析调研：检索台积电/三星/应用材料/PDF Solutions/智现未来等公开材料
- 新增 `docs/方案/MES-AI集成产品分析.md`：结论 = 先 Agent(L0数据暴露+L1 Copilot/报告/RAG-K0)→再检控(L2良率/建议Hold)→后重模型(L3 PdM/VM 等数采)；不推翻既有三篇方案，只做排序
- 新增 `docs/架构/MES-服务架构演进决策.md`：不上 Spring Cloud，维持模块化单体；Agent 服务独立进程/独立仓库（Java + Spring AI/LangChain4j 推荐），REST Tool Facade + RocketMQ 集成；给出拆分触发条件表（Adapter/数采必然独立、Track 事务核永不拆）
- 决策文档升 v0.2 补 ERP/APS 接入章节：ERP=外部集成边界走防腐层（单体内 erp-adapter 模块起步，最终一致+对账，禁跨系统强事务）；APS=独立计划服务（对齐 APS 方案稿：独立部署另库、软约束不挡过站）；治理组件按需单件引入（Gateway 起步），Spring Cloud 是零件库不是目的地
- 决策文档升 v0.3 补 OA/SSO 章节：OA=外部协作系统（同 ERP 防腐层模式，审批/通知/人员同步/待办四场景，审批真相留 MES，回调须验签+幂等）；SSO=横切身份基础设施非微服务（优先接集团 IdP，无则 Sa-Token SSO Server 过渡、长期独立 IdP 禁焊死在 MES），应在外围服务落地前拍板；纪律=认证归 IdP、授权留本地 RBAC、人员主数据单向流
- 决策文档升 v0.4 补前端形态章节（D11~D14）：单仓库多应用（apps/admin·field·dashboard + packages/ui·api-client·types），演进=先 Vite 多入口（成本≈0）→ pnpm workspace；不上微前端；APS 甘特由 APS 服务自带；Copilot 属 MES 前端调 Agent SSE；现场台只读可缓存、写操作必须在线；新增红线 P11（微前端）P12（现场台离线写）
