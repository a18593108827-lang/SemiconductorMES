# 2026-09-20 工作日志

## AI 原生 SDLC 文档体系重构方案立稿
- 背景：用户要求对齐 Anthropic《AI 原生 SDLC 实战手册》（2026-08-21 发布）重构文档格式
- 核实现状：docs 91 篇 md 体系完整（前半条产物链已原生）；缺口在构建之后——server 无 src/test、无 CI、无根级上下文文件、无切片 plan
- 产出：`docs/架构/MES-AI原生SDLC文档体系重构方案.md`（评审稿）
  - 新增：`docs/intent/`（INT 编号意图）、切片级 `{切片号}-plan.md`、根级 `AGENTS.md`、`docs/_templates/` 四模板、`docs/eval/`（后置）
  - 统一 frontmatter 六字段（type/module/status/slices/aligns/updated），blockquote 头部保留
  - 实施切片 Doc-1（模板+AGENTS.md）→ Doc-2（存量补 frontmatter，只加头不动正文）→ Doc-3（Agent 数据暴露需求全链试运行）
  - 关键决策：不重命名存量目录/文档（P1）、不回填历史 plan（P4）、plan 未批不动码（A2/G2）
- 待用户评审批准后执行 Doc-1

## 方案已批准，Doc-1 / Doc-2 实施完成
- Doc-1 ✅：根级 `AGENTS.md`（铁律+文档流程+一页约束）；`docs/_templates/` 四件模板（INT/plan/EVAL/AGENTS骨架）；`docs/intent/`、`docs/eval/` 目录占位（README）
- 方案文档升 v1.1：状态改已批准、补 frontmatter、type 枚举扩充（+业务清单/UI/WI/INDEX）
- Doc-2 ✅：脚本 `.workbuddy/scripts/add_frontmatter.py` 批量补 frontmatter —— 92 篇全覆盖、零遗漏、只加头不动正文（保留 BOM/换行符）；type 分布：接口设计19/架构14/功能清单13/已完成13/功能文档11/数据库10/方案5/WI3/业务清单2/UI1/进度1；状态：done 84 / draft 6（方案类）/ in-progress 2
- `docs/INDEX.md` 总账生成（自动可再生成，--reindex）
- 实施进度文档登记 Doc-1~5 切片 + 新文档入口表
- 注意：本机 Bash 工具损坏（ls/command not found），PowerShell stdout 不回显 —— 脚本输出走文件再 Read
- 下一步 Doc-3：Agent 数据暴露（Tool Facade）需求走 INT→规格→plan→交付 全链试运行

## 项目根目录清理
- 删除三个未使用目录（均确认为可再生脚手架、无任何引用）：`_bmad/`（BMAD 框架安装物，config.user.toml 无自定义内容）、`_bmad-output/`（空）、`design-system/`（仅剩过时索引，真相在根级 DESIGN.md）
- `.workbuddy/` 保留（项目数据非缓存）
- 环境注意：本机 Remove-Item 被 safe-delete 守卫拦截（先移回收站、失败则 FAIL_CLOSED 保留且错误被吞、退出码仍 0）——删目录须逐个 try/catch + Test-Path + Read 三重验证
- git 仓库存在，删除可通过 git 恢复（若已跟踪）

## 客诉追溯包（complaint）状态核查 + 文档补账
- 核查结论：CP-1（DDL/权限/开关/骨架）+ CP-2（preview + 三项摘要）代码与文档一致已落地；CP-3~6 未做；前端零代码
- 补账：`MES-History已完成功能.md` 补登记 CP-1/CP-2 完成记录（§6 客诉追溯包，含依赖检查项）
- 新增 `docs/模块/History（履历）模块/CP-3-plan.md`（draft）：build 落库 + 包头审计 + 装配 VO + GET 详情；零表变更；复用 flattenImpact/genealogy/HistoryFacade.listByLot/HoldService.listByLot；package_no UK 重试 ≤3
- INDEX.md 重建：93 篇
- 架构审查 plan（R1~R8）：R1 装配禁入事务 / R2 UK 重试整体重跑+NO_CONFLICT 错码 / R3 单 VO / R4 履历尾端截断 / R5 create_by 走 StpUtil 安全包装（HoldServiceImpl:267 同款）/ R6 设计缺 list 端点 / R7 成员固定排序 / R8 双遍历可接受
- 用户选 R6=a：list 端点并入 CP-3；设计文档同步修订：§3 切片表、新增 §6.6 List + §6.7 事务边界说明、§9 补 NO_CONFLICT、§12 补验收 10
- CP-3-plan.md 重写吸收全部审查意见（draft 待批）
- 用户手改 plan 增 R9~R14（Writer/Allocator/Assembler 拆分、max(seq)+1、装配失败隔离、AlarmFacade.listUnclearedForLots、build 复用树）；二轮审查确认 R13 可行（flattenImpact 内部已建树，MesLotServiceImpl:253）并修 4 处：①冲突捕获移到 Facade（Writer @Transactional 内 catch 会撞 rollback-only）②纯 seq++ 同频碰撞 5 并发耗尽→改重读 max+抖动 ③listUnclearedForLots 定序 lastRaiseAt 倒序 ④装配失败必记 WARN 日志
- 三轮复核通过：数字后缀取 max（禁 ORDER BY 字符串排序）、build() 禁带事务、Hold 拆 active/released（实体注释核实）、设计 §8.3 已对齐；验收 6 改「唯一为硬条件、NO_CONFLICT 不当不变量」
- 三轮遗留流程欠账已补：`MES-LotGenealogy接口设计.md` §7.4 登记 MesLotImpactFlatVO.tree；`MES-Alarm架构设计.md` §4 登记 AlarmFacade.listUnclearedForLots；INDEX 重建
- **CP-3 plan 已批准**（2026-09-20 用户批准，status: approved）——可动码：DTO/VO → AlarmFacade.listUnclearedForLots → Allocator/Writer/Assembler → Facade build/get/page → Controller → history-per-lot 配置
- **CP-3 代码落地并核验通过**：R1~R14 逐项静态核验全 ✅（Writer 独立 @Transactional / Facade 无事务 / selectMaxSeqOfDay 数字后缀 / 冲突重读 max+nextInt(3) / 装配四块 try-catch+WARN / 成员排序 / flatten:278 挂树 / listUnclearedForLots entity=LOT lastRaiseAt DESC,id DESC）；依赖检查 grep 零命中；`mvn compile` 通过；application.yml 三配置键齐（enabled 默认 false）；A3 三件套（已完成功能/设计/进度）用户已同步，INDEX 重建
- 遗留：运行时验收（curl build→get 比对、并发验号唯一、人为触发回滚/装配失败）需起服务执行；CP-4 export+Admin、CP-5 contain、CP-6 ZIP 待做
- complaint 模块全量补注释：实体（状态/方向/关系枚举常量 + 全字段）、VO（VO/ListVO/LotHoldsVO/PreviewVO/MemberVO 字段）、FacadeImpl + Assembler + Allocator + Writer 全方法 Javadoc、常量块分组注释；仅注释零逻辑改动，mvn compile 通过
- CP-3 性能评审（用户提问触发）：装配四块中履历/Hold/Alarm 均已批量化（listByLots / listUnclearedForLots 各一次 IN，HistoryTxLogMapper 走窗口函数），仅谱系遍历与 Writer 逐条 INSERT（≤200，单事务一次提交）为循环打库；估算本机 DB ≤60ms、跨机房 ≈400ms
- **决策：先不优化，等真实负载观测**（记入 CP-3-plan.md §8 遗留：触发条件 = insert/assemble 段实测 >500ms；方案 = 多值 @Insert + foreach，参照 HistoryTxLogMapper 先例；谱系遍历不建议改）
- Git 现状核查（用户提问触发）：origin = GitHub `a18593108827-lang/SemiconductorMES`，**仓库为 public**；main 与 origin/main 同步于 891bd48；提交身份 guocong <a18593108827@163.com>
- 发现待提交积压 54 项：`com/mes/{complaint,carrier,dashboard,report}` **整个模块目录从未提交**（历史里 carrier/report/complaint 的提交实际只含 .md 文档），且跟踪文件跨切片混改（AlarmFacade/HistoryFacadeImpl/HoldServiceImpl/application.yml）
- 发现敏感项：`server/src/main/resources/application-dev.yml` 已提交公开（本机 MySQL 弱口令 + Redis 内网 IP 192.168.187.128），.gitignore 未排除；建议转 private 或改环境变量占位
- **提交完成（两刀）**：`4630221` 补交其他模块（carrier/report/dashboard/alarm P1/track/wip/lot/history/hold，75 文件 +3604）；`b2c6c81` 客诉追溯包 CP-1~3（complaint 全模块 + 上游 Facade 扩展 + 文档，28 文件 +1595）；工作区已干净
- **推送受阻（环境限制）**：本机 `github.com:443` 不可达 —— 宿主代理 `127.0.0.1:64411` 对 github.com 返 `502 CONNECT tunnel failed`，直连 21s 超时，`api.github.com` 却 200（白名单式放行）；探测常见本地代理端口（7890/7897/10809/10808/1080/8889/2080/33210）全部不通。**结论：须在用户自带代理/VPN 的客户端（IDEA 或本机 Git Bash）执行 `git push origin main`**；沙箱内外均无解
- **发现嵌套仓库 `server/.git`（双仓库根因）**：该目录是独立 Git 仓库，remote = `github.com/a18593108827-lang/server.git`（public，默认分支 master），共 **91 个提交且全部已推送**（0 未推送、无分叉），工作区 23 个改动即当日 CP-3 代码。这解释了「代码提交进 server 仓库、文档提交进 MES 根仓库」的双历史现象；同一批文件被两个仓库同时跟踪
- **清理动作完成**：用户关闭 IDEA 后 `server/.git` 成功移至 `/d/java/xm/2026_07/_git-backup/server-dotgit-20260920`（保留备份，未 rm）。校验：全仓仅剩 `./.git`；在 `server/` 下执行 git 已归属根仓库 `SemiconductorMES`（单仓库生效）；根仓库 HEAD `ee90b23` 完好
- 长期记忆新增「Git 仓库约定」章节（单仓、双仓历史根因、提交风格、推送环境限制、application-dev.yml 待处理）；远端 `a18593108827-lang/server` 仓库仍在 GitHub（91 提交），是否归档/删除由用户决定
