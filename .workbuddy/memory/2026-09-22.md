# 2026-09-22 工作日志

## MES 实践固化为用户级 skill：ai-native-sdlc
- 背景：用户希望把 AI 原生 SDLC 手册做成 skill，在任何项目直接生成/执行规范；确认设计为「bootstrap（接入生成）+ enforce（日常阶段路由）」双模式，技术栈无关
- 产出（`~/.workbuddy/skills/ai-native-sdlc/`）：
  - SKILL.md：模式判定 → bootstrap 六步（扫描/访谈 3~5 问/生成四模板+AGENTS.md/可选批量接入/DoD/等批准）→ enforce 阶段路由表 + 治理铁律；触发词覆盖「新需求/加功能/开始写代码/修 bug/复盘/生成规范」
  - references/templates.md（通用版四模板 + frontmatter 六字段 + 目录路由）、references/playbook-core.md（手册原则精炼：瓶颈转移/产物即审计/三层控制/人类角色/衡量指标）
  - scripts/add_frontmatter.py：配置驱动通用版（--init-config / --apply / --index），已冒烟测试通过（module 正则提取、状态启发、BOM/换行保留、INDEX 重建均正确）
  - 验证打包通过：`~/.workbuddy/skills/dist/ai-native-sdlc.zip`（skill-creator 官方校验）
- 关键设计决策：方法论进 skill、项目特定内容（铁律/技术栈/目录）由 bootstrap 访谈生成；skill+项目 AGENTS.md 构成建议性控制层，CI 属强制层（超出 skill 范围）
- 已登记用户级 MEMORY.md（跨项目事实）

## 竞品调研 + README 生态定位（同日）
- GitHub API 实测调研：同赛道直接实现 Anthropic 手册的仓库约 6-8 个；最成熟 bashebr/ai-native-sdlc（64★，图工程/审批账本/Agent 组织，但 greenfield + 全英文）；大框架生态 Spec Kit 93k★ / BMAD 46.7k★（即删掉的 _bmad）/ superpowers 289k★ / OpenSpec / GSD / potpie 5.7k★；WorkBuddy 内置市场无同类
- README 增补「与现有方案的区别（生态定位）」章节（5 方案对比表 + 选型建议 + 格式同源说明），commit `2a57ac5` 已推送（第二次推送沿用绕过 GCM 的方法，exit 0；远程 HEAD 与本地一致，提交信息中文正确）

## MES 客诉包 CP-6（ZIP 导出）切片计划起草
- 现状核实：History 模块一期 + 调查台 + 客诉包 CP-1～CP-5 已完成，唯一挂账 = CP-6 ZIP（`MES-History已完成功能.md` 2026-09-21）
- 对码结论（写进 plan 前提）：`ComplaintPackageFacade.exportFile(Long,String)` 已存在，链路 = assertEnabled → assertFormatJson → get → ObjectMapper 拼 ObjectNode(+exportedAt/exportedBy)；Controller 已泛型 `ResponseEntity<byte[]>` + octet-stream + `ContentDisposition.attachment().filename()` 且映射未写 `produces` → **CP-6 Controller 零改动**；前端 `downloadFile` 按 Content-Type 前缀分流，zip 走二进制分支天然可用
- 产出：`docs/模块/History（履历）模块/CP-6-plan.md`（status: draft，未批不动码）；口径 = `format=zip` → `{packageNo}.zip` 含**恰好 2 entry**（`{packageNo}.json` + `README.txt` 封面），扁平 ASCII 名 + UTF-8；抽 `ComplaintPackageExporter`（support）承载 JSON 组装/README 渲染/ZIP 打包，Facade 只编排 + format 白名单校验
- 关键约束：K3 format 校验先于 get（禁 404 抢错误码）· K4/K5 JSON 与 zip 内 JSON 同源同 ObjectMapper 且 `now()` 只取一次 · K6/K8 扁平 entry + `ZipOutputStream(UTF_8)` + 必须 close + setTime 非零 · K9 Exporter 零 Mapper · K10 大包 >10MB 只 WARN 不加错误码 · 决策 D1 README.txt 代 PDF 封面（零新依赖，PDF 进遗留）
- 待办：等用户批准 plan（批准记录行写明批准人/日期）→ 动码 → 同会话同步接口设计 §3/§6.4 + 已完成功能（slices 加 CP-6）+ 进度文档三处「ZIP 后置」+ INDEX 重建

## CP-6 plan 二轮源码级审查（同日，用户更新文档后）
- 用户自行扩写了 plan（K13/K14、D4~D6、C10~C13：no-store、setTimeLocal、封面口径、失败面单点翻译、跨请求只比 keys）→ 我做源码级对码 + JDK 21 实测，产出 F1~F9 / C14~C16 / K15 / D7 / D8，并已回写 plan
- 实测（临时 javac 探针，JDK 21.0.10）：`ZipEntry.setTimeLocal` 可用；**ZIP DOS 时间 2 秒粒度**——`setTimeLocal(10:48:17)` 回读 `10:48:16` → 验收禁做秒级相等断言（否则误判 bug）；`close()` 内 finish 后 `toByteArray()` 得 2 entry / 289 字节正常
- 对码结论：`JacksonConfig` 仅定制 Long→ToStringSerializer，LocalDateTime 走默认 JavaTimeModule ISO 文本 → K5「README 文本 = JSON 文本」成立，JSON 里 `exportedBy` 为字符串；`ComplaintPackageAssembler` 现网已绑定 `history-per-lot` 且已注入 FacadeImpl → 删掉 plan 里 FacadeImpl 重复 `@Value`，改 `assembler.historyPerLot()` 访问器（K15/D7）；README 成员数定标 `vo.getMemberCount()`（成员取自表、不漂；VO 无包头 member_count 字段）（D8）；zip 路径内存峰值 ≈ 3N（json N + zip 缓冲 N + toByteArray 拷贝 N）→ 并入 K10；Exporter 需 `@Slf4j` 才能落 K10 WARN；Facade 分支必须用归一值 `normalized`（`" ZIP "` 最易写错）
- 前端核实：`exportComplaintPackageApi` 仅 `ComplaintPackageDrawer` 调用（HistoryPage / LotsPage 只是打开同一抽屉）→ plan §4 前端改动面正确

## CP-6 plan 三/四轮审查（同日，用户两次改文档后）
- 用户三轮补入：D9（Hold/告警上限走访问器）、F10–F12、K15 扩为三个上限访问器、K8 明确 `setTimeLocal` 先于 `putNextEntry`、K10 内存含 VO + ObjectNode、K14 三行上限、验收 9/13 改代码阅读（丢掉"临时改常量造 WARN"）
- 我的四轮复核（实测 + javap 反编译 + 对码），产出 F13–F15 并回写：
  - **F14（实测，最重要）**：`setTimeLocal` 晚于 `putNextEntry` → 本地头写当前时间、中央目录写设定值，**同一 entry 两个头时间不一致**（probe：late.txt 本地头 2026-09-22T11:03:50 / 中央目录 2000-01-01T00:00；early.txt 两头一致）→ 验收 2 增两头一致性检查
  - **F13（反编译）**：`ObjectMapper.writeValueAsBytes` 内部 `new ByteArrayBuilder` → `_writeValueAndClose` → `toByteArray()`（字节码 `newarray` + 逐块拷贝）→ JSON 路径字节侧 ≈ **2N**（原 K10 写 1N 低估）→ 改为「JSON 2N / ZIP 3N，并发预算按 3N」
  - **F15（对码）**：`ComplaintPackageAssembler:171` 确有 `int cap = historyPerLot < 1 ? 100 : historyPerLot;`，下游 `historyFacade.listByLots(keySet, cap)`（一次 IN + 窗口函数）→ K15「访问器返回生效值 + 删方法内钳制」成立且访问器值 = 实际查询值；`HOLD_CAP`/`ALARM_CAP` = 20（active/released 各自 20、每 Lot 未告警 20）确认
- 三轮其余判断均正确（F10 生效值钳制、F11 时序、F12 常量分叉风险、K13 空块说明与现网"整块失败→空盒 + WARN"一致）
- 同步给 `ai-native-sdlc` skill 补 enforce 路由行「再审查一遍 / 我改了文档」+ 治理铁律两条（plan 定稿≠正确，审查需行号/实测/反编译证据；多轮审查只追加 F/C 行不静默改写）——今天两次复用同一模式，属真实缺口

## CP-6 plan 状态：draft → approved（2026-09-22 用户批准）
- `docs/模块/History（履历）模块/CP-6-plan.md`：frontmatter `status: approved`、头部状态行 `**approved**`、批准记录写入「2026-09-22 用户批准」+ 四轮审查摘要（D1–D9 / K1–K15 / C1–C16 全量生效）
- 计划实施顺序（已按此于当日全部完成，见下节）：按 §5 顺序 a→h 实施——a Assembler 三访问器（`historyPerLot()` 生效值 + `holdCap()`/`alarmCap()`，`loadHistories` 改调访问器删方法内钳制）→ b 新增 `ComplaintPackageExporter`（`@Component`+`@Slf4j`，只注入 ObjectMapper，先 `setTimeLocal` 再 `putNextEntry`）→ c Facade `exportFile`（assertFormat 白名单 json/zip + 归一值分支 + try 只包 Exporter）→ d Controller `.cacheControl(noStore())` → e 清 FacadeImpl 旧序列化逻辑 → f/g 前端 api + 抽屉双按钮 → h 文档收尾（接口设计 §3/§6.4、已完成功能 slices 加 CP-6、进度文档三处 ZIP 后置、`python .workbuddy/scripts/add_frontmatter.py --reindex`）

## CP-6 实施完成（2026-09-22，commit 6595d83）
- 后端：新增 `support/ComplaintPackageExporter`（JSON 节点 / README / ZIP / 文件名，只注入 ObjectMapper）；`ComplaintPackageAssembler` 加 `historyPerLot()`（生效值）+ `holdCap()` + `alarmCap()` 且 `loadHistories` 改调访问器（删方法内钳制）；Facade `assertFormat` 归一白名单 json/zip + 按归一值分支 + try 只包 Exporter；Controller export 加 `Cache-Control: no-store`
- 前端：`exportComplaintPackageApi(id, fileName?, format='json')`；抽屉 `downloading: 'json'|'zip'|null` + footer「下载 JSON」/「下载 ZIP」
- 验证手段（无测试基建 + 环境不可用时的可复用套路）：写**一次性探针** `.workbuddy/tmp/cp6-probe/Cp6Probe.java`——真 `SpringApplication` 起容器取真 `ObjectMapper` Bean（排除 DataSource/MybatisPlus autoconfig）+ 直接 `new ComplaintPackageExporter(om)`，12 项断言全 PASS：entry 恰 2、**本地头 == 中央目录时间**、**ZIP 内 JSON 与 `toJsonBytes` 逐字节相同**（比 plan 的「代码阅读」更硬）、README 20 项行项、时间与 JSON 同值同形、Long→字符串。`mvn -o -DskipTests compile` EXIT=0、`npm run build`（tsc -b + vite build）通过
- 踩点（重要）：`LocalDateTime` 序列化形状取决于**容器 builder**——手搓 `Jackson2ObjectMapperBuilder.json()` 关不掉 `WRITE_DATES_AS_TIMESTAMPS`（出 `[2026,9,22,...]` 数组），而 Spring Boot 自动配置的容器 ObjectMapper 出 ISO 文本 `"2026-09-22T09:30:00"` → 探针必须用真容器 Bean，否则会误判代码有 bug（本次先误判、后定位）。已把 `scalar()` 加固为「非标量节点回退 `node.toString()`」，避免封面静默印 `-`
- 环境事实：本机 MySQL 3306 在；**dev Redis `192.168.187.128:6379` 当前不可达** → 服务起不来，验收 1/5/6/7/11 的真机 curl 未执行（已在 plan 实施记录登记「未执行 + 补做条件」，并留 task #7）
- 文档同步：接口设计（§3 范围/切片表、§6.4 补 ZIP 契约、§9 错误码措辞、§14 实施备注、头部状态）、History 已完成功能（slices 加 CP-6、§6 CP-6 ✅、新增 §7 导出）、进度文档三处「ZIP 后置」→ 已落地、INDEX 重建（96 篇）；plan 追加「实施记录」（依 CP-3/4/5 惯例 plan 保持 `approved`，完成态落「已完成功能」）
- skill 更新：`ai-native-sdlc` enforce 路由「实现中/实现完」行补入「无测试基建或环境不可用时写一次性探针 + 环境阻塞条目显式登记未执行」；已重建 `~/.workbuddy/skills/dist/ai-native-sdlc.zip`（同步含今日两处 SKILL.md 改动）

## CP-6 真机验收补做（2026-09-22 15:24，Redis 恢复后）
- 环境：Redis `192.168.187.128:6379` 可达、后端 `127.0.0.1:8080` 在跑（用户起，未重启）、文档登记的 dev 账号 `admin/123456` 可登录；接口无 `/api` 前缀（vite 代理才带）
- 结果：**HTTP 层 26 项断言全 PASS**（`.workbuddy/tmp/cp6-probe/http_acceptance.py`，可重跑）：zip 头（octet-stream / no-store / filename）与 `PK` 魔数；恰 2 entry；本地头==中央目录时间；ZIP 内 JSON 与独立 json 除时间戳外逐字段一致；README 20 项行项齐且导出时间与 JSON 同值同形；`format=pdf` → 业务码 FORMAT 且零 zip 字节；`format=pdf&id=不存在` → FORMAT 而非 NOT_FOUND（K3 成立）；4 个存量包全出 2 entry；json/空 format 口径不变
- **口径实测（重要，已写进接口设计 + 项目记忆）**：业务异常 `code` 恒 500，**业务码在 `msg` 前缀**（`GlobalExceptionHandler` → `R.fail(e.getCode(), …)`）→ 我第一版脚本按 `code` 断言导致 5 项误报 FAIL，属探针 bug 不是产品缺陷
- **探针自身两处误判（已修）**：① 本地头 DOS 时间秒字段是 `seconds/2`，未 ×2 与 python `date_time` 比对 → 误报不一致；② 业务码取错字段
- **G1 前端缺陷（用户实测发现并修复，待我提交）**：CP-6 抽屉拆双按钮后缺重入保护——同时点两钮会并发下载、先完成者的 `finally` 清掉后者 loading、抽屉重开后旧请求 `finally` 清新会话状态；修法 = `downloadingRef` 同步重入闸 + `downloadSeq` 会话序号 + 任一下载在途时两钮 disabled，已过 `tsc -b`。责任在 CP-6 前端实现（CP-4 单按钮不暴露）
- 仍未验证：`enabled=false → COMPLAINT_PACKAGE_DISABLED`（需改配置重启，避免打断用户实例）；`docs/eval` 仅 README，EVAL 流程未启用 → G1 是否走 EVAL 待用户定

## EVAL 流程启用（2026-09-22，用户批准「开始启用」）
- 首条落档：`docs/eval/EVAL-0001-追溯包抽屉并发下载重入.md`（G1 前端缺陷）——含触发条件/复现/预期行为/根因（state 异步窗口 + finally 无会话归属）/修法三手段（`downloadingRef` / `downloadSeq` / 双钮 disabled）/回归校验（手工 + 代码断言 + tsc）/防复发回流点
- 启用落地（「可逐环启用」套路）：① 建首条产物；② 重写 `docs/eval/README.md`（启用日期 + 落档口径表「生产事故必落 / 验收期缺陷也落 / 探针误判不落、暴露口径偏差则改文档」+ 回流要求「只修码不回流 = 未闭环」）；③ 清掉 AGENTS.md §4-5 与方案文档中的「后置启用」字样（重构方案 4 处）；④ `docs/_templates/plan-模板.md` §6 增「副作用与并发」验收栏；⑤ INDEX 重建（97 篇，含 EVAL-0001）
- CP-6 plan 的 G1 行与「遗留」段同步指向 EVAL-0001
- skill 回流：`ai-native-sdlc` enforce 表把「线上事故/修 bug/复盘」扩为「含验收期、联调期缺陷」并要求「每条 EVAL ≥1 个防复发回流点」；新增「启用某个后置环节」一行（五件套）；治理铁律补两条（产物链可逐环启用、启用即清「后置启用」字样；验收项必须覆盖副作用与并发）；重建 dist zip
