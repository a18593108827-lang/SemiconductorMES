# 2026-09-23 工作日志

## 厂型决策落定：后道封测 + AI 赋能并行（commit 63195c3）
- 用户拍板：「侧重后道的完善以及 ai 的赋能」，前道留待产线成熟后转型
- 决策落地五处（全部已提交）：① `PRODUCT.md` — Positioning 增「目标厂型（2026-09-23 定）」、Users 收敛为「后道封测厂为主」、约束拆三类（片级/SlotMap 远期 · 颗级+测试分档为当前主线 · AI 只读先行且不得直改状态）；② `docs/方案/MES-厂型选型分析.md` v1.0 定稿（status done + §0 决策记录：日期/决策/决策人/落点 + §7 标「已执行」）；③ 进度文档 §2 增「下一步（三轨：主线封测闭环 / 并行 AI / 远期前道）」、§6 厂型项标「已定（2026-09-23）」；④ 业务清单 §16 三项核心 P1→P0、§17 降远期；⑤ INDEX 重建 100 篇
- **首两份意图**（`docs/intent/` 此前仅 README）：`INT-0001-封测颗级追溯与测试数据回流.md`（主线：颗级/Strip 层级 + 测试分档 Bin 回流 + 不良 Bin→Hold/Rework + 客诉包延伸到颗级；5 条验收口径 + A1~A5 约束 + 5 个未决）、`INT-0002-AI赋能只读数据暴露与摘要查数.md`（L0 只读数据暴露 → L1 摘要/查数；A1~A5 含"不得跳过 L0"与"数据不出内网"）；两份均 draft
- 我给的产品判断（写进文档与决策记录）：后道是"顺风位"而非退让（内核=批次流转+追溯+质量拦截）；AI 赋能不得跳过 L0 地基；转型前道的现实路径是 8 寸/特色工艺 + Adapter 能力，而非慢慢平移
- **踩点（重要）**：`docs/方案/MES-厂型选型分析.md` 被外部格式化器改写——首行 `---` 被转义成 `\---`、每行尾追加两个空格 → frontmatter 解析失败 → INDEX 状态回落成 `draft`（实际 frontmatter 写的是 done）。已整篇重写干净版并全仓扫描确认无其它文档受影响。**教训：Write 落盘的新 md 可能被 IDE/格式化器改造，之后 Edit 会 MISMATCH；改动前先 Read 最新内容，改完用字节级检查（首行是否 `---`、行尾是否多空格）+ 重建 INDEX 校验状态列**
- 本轮结束后待推提交：4（63195c3 起算，含昨日 3 个）

## 新建业务知识库 + 首篇 BK-0001（commit 163674b）
- 用户诉求：把 Bin 的业务知识写成文档，并新建一个**业务知识文件夹**长期积累，便于查阅/熟悉/复习巩固
- `docs/业务知识/`（新目录）：README.md（**无 frontmatter**，与 `docs/intent/README.md`、`docs/eval/README.md` 同例——目录导航页）约定：用途（回答「是什么·为什么」，与技术实现解耦，正文不引用本系统表名/类名）、与 intent/方案/模块 的**边界表**、命名 `BK-{N}-{主题}.md` 四位递增、frontmatter 六字段（`type: 业务知识`，`status: active/draft`）、正文建议结构（一句话定义 / 为什么重要 / 关键概念含中英对照 / 常见误区 / 与 MES 的关系 / 自查问题 / 待核实 / 关联）、复习方式（先读定义+自查问题，答上就跳过正文）、已收录清单、**待补清单 12 项**（Wafer Map、CP/FT 区别、良率指标族、SPC/CPK、8D/PPAP/IATF、Strip/Die/Reel 载具族、OEE、Q-Time、STDF、SECS/GEM、换型、委外）
- `BK-0001-测试与Bin分档.md`：定义（Bin=桶/档位，号越小越好）、重要性五视角、CP vs FT 对照、**soft bin vs hard bin**（软定责/硬定位，一个 hard 合并多个 soft）、Bin Map 图形判读表（边缘/中心/对角/随机）、Bin 汇总示意（含"同良率不同构成"洞察）、6 条误区、与 MES 的关系（绑定/良率/拦截/证据链，YMS 属后置边界）、6 个自查问题、4 项待核实
- 路由与索引：AGENTS.md §4 新增「业务知识」条（后续编号顺延）；进度文档 §4 增「业务知识库」入口；INDEX 脚本 `TYPE_ORDER` 增「业务知识」→ INDEX 重建 101 篇，出现「业务知识（1 篇）」分组
- 可复用约定：**新增「业务知识」类知识一律进 `docs/业务知识/BK-{N}-*.md`**，frontmatter 写 `type: 业务知识`（INDEX 的 type 以 frontmatter 优先，脚本目录启发式兜底）；导航页 README 不带 frontmatter

## Doc-4 最小回归网落地（2026-09-23，commit 1ef4339）
- 前置事实（实测）：`server/src/test` 不存在、pom 无任何测试依赖、`.github/workflows` 不存在；本地仓库**已有** `spring-boot-starter-test:3.3.5` → 可离线构建
- plan：`docs/架构/Doc-4-plan.md`（status approved，批准依据 = 用户「先做现在的回归网」+ Doc-4 在重构方案中已批）；§实施记录含验收结果与 F1~F4
- 产出：pom + `spring-boot-starter-test`(test scope)；3 个测试类共 **12 用例**（客诉包导出装配 6 / 上限生效值 3 / format 白名单 3），全部不依赖 MySQL·Redis·网络；`.github/workflows/ci.yml` 三闸门（后端 `mvn -B test`、文档 reindex 后 `git diff --exit-code docs/INDEX.md`、前端 `npm ci && npx tsc -b`）
- **验收 12/12 绿 + BUILD SUCCESS**；`dependency:list -DincludeScope=runtime` 中 junit/mockito/assertj 命中 **0**；**零生产代码改动**
- **反向验证**（关键手法）：临时把 `setTimeLocal` 挪到 `putNextEntry` 之后 → `localHeaderTimeMatchesCentralDirectory` 立刻变红 → 证明断言不是摆设；随后 `git checkout` 还原
- 实施期事实：F1 `@JsonTest` 直接加载 `MesApplication` 会因全量扫描带出 MyBatis mapper 而上下文失败 → 改用测试内嵌最小 `@SpringBootConfiguration` + `@EnableAutoConfiguration` + `@Import(JacksonConfig.class)`；F2 AssertJ 比 `List<int[]>` 恒不等 → 改 `List<Integer>`；F3 手搓 ObjectMapper 的坑在测试里同样咬人；F4 docs 门禁（reindex+diff）**顺带覆盖** frontmatter 被格式化器破坏（状态列回落 → diff 非空）
- 文档同步：进度文档 §5（Doc-4 ✅ / Doc-5 ✅）、`docs/eval/README.md`（回归网就位，后续 EVAL 回归段落成用例）、INDEX 102 篇
- 同日再次发生同一格式化器破坏：`docs/方案/MES-厂型选型分析.md` 被二次改写（首行 `\---` + 行尾空格）→ 用 `git checkout --` 从已提交的干净版本还原；**该破坏会被 CI docs 闸门在下次推送时兜住**

## EVAL-0001 前端回归网落地（2026-09-23，commit 4982d6d）
- 用户选方案 A（完整行为用例 + CI 接入）；plan：`docs/架构/EVAL-0001-plan.md`（approved，含实施记录与 F1~F4）
- 引入 devDeps：`vitest@5` / `jsdom@30` / `@testing-library/react@16` / `user-event@14` / `jest-dom@7`（npm registry **直连可达**，无需代理；`dependencies` 未动，react 19.2 / vite 8.1 未被改）；`web/vitest.config.ts` + `src/test/setup.ts`；`package.json` 加 `"test": "vitest run"`；CI web job 加 `npm test`
- `ComplaintPackageDrawer.test.tsx` 两条行为用例（mock api/hold/Toast/Confirm/Drawer 成最小容器）：① 同一 `act` 内连点双钮只发 1 个请求 + 在途两钮禁用；② 抽屉重开（换 anchorLotId）后旧请求收尾不得清新会话下载态。基线 **2 passed**，`tsc -b` EXIT=0，生产代码零改动
- **反向验证两条都命中**：摘 `downloadingRef` 早退 → 用例1 报 `expected "vi.fn()" to be called 1 times, but got 2 times`；摘 `downloadSeq` 比较 → 用例2 报 `Unable to find ... "处理中…"`（新会话 loading 被清）→ 断言是真保护
- **关键踩点（F1，最有价值）**：**第一版用例用 `user.click` 连点两钮，摘掉 ref 闸门后仍全绿**——因为 `disabled={... || downloading != null}` 已锁住第二钮，user-event 尊重 disabled 不派发点击。真正的竞态窗口是「React 提交之前」，必须用同一 `act()` 内 `dispatchEvent(new MouseEvent('click'))` 连发两次才复刻得到 → 教训：**反向验证是最有效的用例质检手段**，不做就发现不了"假保护"
- 其它踩点：F2 `Button` loading 时用「处理中…」**替换 children** → 在途断言要按该文案找（顺带成为"是否仍在下载"的探针）；F3 vitest 未开 `globals` 时 RTL **不会自动 cleanup** → 第二个用例报 `Found multiple elements`，须在 setup 里显式 `afterEach(cleanup)`
- 文档：EVAL-0001 回归校验段改为「自动化优先 + 反向验证证据」、进度文档 Doc-4 行（前端 tsc + 行为用例）、INDEX 103 篇

## INT-0001 采纳 + 封测跨模块规格 + TD-1 plan 草案（commit 0fffa61）
- 用户问「下一步推荐做什么」→ 判断：16 模块全绿、主路径与客诉链闭环，但**厂型已定后道封测而封测特化能力为 0**（追溯只到「批」、Bin 数据全在系统外、不良处置靠人）→ 这是厂型决策落地后最大缺口；推荐 INT-0001 主线，用户选线并拍板 5 项未决
- **决策 5 条（全部关闭，落 INT-0001 §6）**：① 颗粒度自 **Strip 条级 + Bin 汇总**起步（依据 `BK-0001` 误区 6：批级+汇总+条级已能支撑良率/审厂/拦截；颗级依赖 wafer map）② 入库 = **测试记录头 + Bin 汇总**，soft bin 与逐颗明细只留文件引用 ③ Bin **独立字典**（不并入 `mes_hold_reason`；Bin 是产品/程序维度且随版本变，Hold 原因是处置维度）④ 客户 Lot 映射**同期落数据模型**、界面后置 ⑤ **本主线先于 INT-0002**（L0 暴露层同期设计、独立交付，先暴露已有模块即可闭环 Doc-3）
- 规格 `docs/方案/MES-封测测试数据与Bin回流方案.md`（跨模块，draft）：核心层级判断 = **Lot 是执行/状态单位，Strip 是身份/位置单位**（Strip **不**建 Lot、不绑 Route、不过站、**不写 tx_log**，否则 WIP/Route/Track 语义崩）；5 张新表（`mes_bin_def` / `mes_test_record` / `mes_test_bin_summary` / `mes_lot_strip` / `mes_lot_customer_map`）；**写入即对账**（`Σ bin_qty(HARD) == total_qty`，不等则拒且零落库）= A3「追溯链不依赖人工补录」的唯一可靠落地手段；客诉包**增块**（`testSummaryByLot`）而非新造导出格式；客户映射**不进 genealogy**（图只认 split/merge）
- **复用现网已就绪件（不新造，重要）**：`mes_route_edge` 的 `rework` 边 + `reason_codes` 字段（TD-2 的 Rework 建议直接用）、`mes_hold_reason` 独立字典（TD-2 Hold 用）、`ComplaintPackageNoAllocator` 的「当日 MAX(数字后缀)+1 + UK 兜底 + 冲突重试」取号思路（新记录号 `TR-yyyyMMdd-序号` 照抄，禁 `COUNT(*)+1` 与 `ORDER BY DESC LIMIT 1`）
- **事实不符（重要，已登记方案 §12 遗留）**：`AGENTS.md` §3 写模块分层 `api/application/domain/infrastructure`，**实测 lot / hold / complaint 现网是 `controller/dto/entity/mapper/service(+impl)/vo`**（另有 facade/support/event/job/listener/aspect/annotation/ws）→ 新模块**按现网分层**，建议把 AGENTS.md 改成实际，否则后续每个 AI 会话都会照错的目标态建模块
- TD-1 plan：`docs/模块/测试数据（Test）模块/TD-1-plan.md`（draft，**未批不动码**），含 **M1~M4 实施前置核对项**：权限 id 段（拟 340–343，**须先 `SELECT MAX(id) FROM sys_permission` 复核**，现网已用至 332）/ 字典全局档填 `''` 而非 NULL（MySQL 唯一索引不收 NULL）/ `schema.sql` 同步方式 / CP-6 README 行项 20→21；负面清单明列不做自动 Hold-Rework、不做颗级、**现场台零改动**；回滚 = 代码回滚 + 5 张纯新表 DROP
- 文档同步：INT-0001 status→approved 且新增 §6 决策记录、5 个未决勾选关闭；进度文档增「INT-0001 切片」表 + §4 入口 + **Doc-3 改「进行中（以 INT-0001 为全链试运行对象）」**；INDEX 105 篇（原 103）
- 验证：reindex **幂等自检 PASS**（连续两次 md5 一致 `b90b917f…` → CI docs 闸门可过）；三份新/改文档 frontmatter 字节级检查（首行 `---`、无 BOM、无转义）全过
- 待推提交：5（本轮 1 + 此前 4）
- 下一步：等用户批 TD-1 plan → 按 §5 步骤 a→g 实施（先做 M1~M4 前置核对）

## 补充（用户决策）：AGENTS.md 分层同步为现网实际（同日）
- 用户就上一轮提出的「`AGENTS.md` §3 分层与代码事实不符」作答：**按现在的模块分层，不要改（代码）** → 执行 = 改 `AGENTS.md` §3 为**现网实际分层**（`controller / dto / entity / mapper / service(+service/impl) / vo`，按需另加 `facade` / `support` / `event` / `job` / `listener` / `aspect` / `annotation` / `ws`），并补一句「新建模块一律按此，禁止自创第二套分层」；**存量代码零改动**
- 同步四处：方案 §4「包结构」段改写（AGENTS.md 已同步，不再是「目标态」口径）· 方案 §12 遗留行标 **已闭环（2026-09-23）** 留痕 · `TD-1-plan.md` K8 措辞对齐 · 项目 `MEMORY.md`「技术栈」条更新
- 通用教训（可复用）：`AGENTS.md` 这类「AI 会话第一入口」文档一旦与代码事实不符，会**稳定误导后续每一个会话**（每轮都照错的分层建模块）—— 发现即当轮澄清并收敛，不要攒着

## TD-1 规格第 1 轮审查（只审不动码）+ 修复被破坏的 frontmatter（同日）
- 用户改写了 `docs/方案/MES-封测测试数据与Bin回流方案.md`（+168/−141，自称「架构审查修订」）：新增 **D11** 号段表发号（`mes_test_record_no_seq`，复用 `MesLotNoSeqMapper` 机制）/ **D12** 防重复靠 V6 业务判重窗口（10 分钟）而非 UK / **D13 + §3.7** 软删与 UK 共存（strip / customer_map 的 `deleted` 改 **BIGINT 置主键 id**、UK 含 deleted）/ **A11** 只可作废不可删改 / `mes_bin_def` 加 `version` 乐观锁 + `bin_scope` 一致性校验 / 接口改 `GET /test/summary/by-lot/{lotId}`（Test 自有 controller，避免 Lot 直连）+ 新增 `PUT /test/records/{id}/void`
- **F1（阻断级，已修）**：frontmatter 又被同一格式化器破坏 —— 首字符变 `\`（`\---`）、首尾行加尾随空格。实测 `text.startswith('---') == False`；索引脚本 `:100`（`body.startswith(b"---")`）`:138`（`text.startswith("---")`）据此判定 → **后果**：① status 流转**静默失效**（改成 approved，INDEX 仍显示启发式值）② 下次 `--apply` 会给文档**再插一个 frontmatter**（重复头）。**关键洞察：本次 reindex「无差异」是巧合（启发式推出同值），不能当作没坏** —— 比上次「INDEX 状态列回落」隐蔽得多。已按字节修复 `\---`→`---`（行尾空格保留，属编辑器风格、不影响解析只污染 diff）
- **C1（最关键待决策）**：D13 与现网**全局逻辑删除硬冲突**。证据：`application.yml:14-18`（`id-type: assign_id` 雪花 + `logic-delete-field: deleted` + `logic-delete-value: 1`）+ `common/BaseEntity.java:25-27`（`@TableLogic private Integer deleted`）+ 25 实体继承 + 全库 deleted **27 处全 tinyint** + UK 含 deleted 的表 **0 个**。冲突两点：(a) MP 的 `logic-delete-value` 是**固定值**，不支持置主键 id → 要置 id 必须手写 UPDATE（等于该表放弃 MP 逻辑删除）(b) `Integer` 上限 21.4 亿 < 雪花 id（19 位）→ **置 id 会整数溢出**。三选一：① 回 tinyint + UK 不含 deleted（「撤销后重登」用 UPDATE 表达）；② BIGINT 但须自声明 `Long deleted` + 手写软删 + 写明全仓唯一例外；③ 物理删（丢审计，与 A6 冲）。**我建议 ①**
- 其它待决策：**C2** 权限码 `test:record:view` 是**全库首个三级码**（实测 `LIKE '%:%:%'` 返回空，现网 50+ 码全两级）；**C3** V6 判重键缺 `eqp_id` → **双机并测会被误拒**（同批/同程序/同时间/同总量、两台测试机并行，场景真实）；**C4** Bin 字典缺 `program_version` 维度 → 与 A9（版本不可空，理由是 Bin 语义随版本变）**存在一致性缺口**
- 引用核实成立（F）：`MesLotNoSeqMapper:15-23` 发号机制准确（补细节：取号**两步** —— `bump()` 拿影响行数、`lastInsertId()` 拿值）；`MesEdcPlanServiceImpl:201-205`「物理删再插躲开软删唯一键冲突」准确；乐观锁**有现成机制**（`MybatisPlusConfig:23` + 7 实体 `@Version`）→ §5 直接复用 `@Version`，不必手写条件更新
- **DB 只读实测（新能力，首次用）**：MySQL **8.4.8**、`@@transaction_isolation = REPEATABLE-READ`（§10 口径成立）;`sys_permission` MAX(id)=**332**（plan M1 拟 340–343 无冲突）；库内**无** test/bin/strip/wafer 表（封测特化确实为零）；`mes_route_edge.edge_type` 实有 `rework(1)/normal(27)/skip_allow(5)/time_link(4)/off_flow(2)`（后两者文档未提，低优先补）
- 环境：managed venv 装 `pymysql` 2.2.8（`C:/Users/Admin/.workbuddy/binaries/python/envs/default/Scripts/python.exe`）；探针落 `.workbuddy/tmp/review-probe/`（不入库，可重跑）
- 处置：frontmatter 修复 + 追加「审查记录（第 1 轮）」节（F1–F8 / C1–C4 + 未覆盖项）；**未改任何设计结论**（C1–C4 待用户拍板）

## C1 裁决落地：软删口径回归现网（commit 1946a4a）
- 用户拍板「按 ① 写进规格」→ 采纳方案 ①（放弃「deleted 置主键 id」，全部按现网口径）
- **落点 6 处**：① §3.7 重写为「**已定：与现网一致**」（含决策依据三处硬冲突 + 逐表口径 + 「MySQL 无 PG 式部分唯一索引，故不存在第三种解法」）② §9 D13 改写（写明原方案冲突与代价）③ §3.1 / §3.4 / §3.5 **字段说明与 UK 行同步**（UK 中的 `deleted` 一并去掉：`uk_record_no` 侧不动，`(lot_id, strip_no)`、`(lot_id, map_type, external_lot_no)`、`(product_code, program_name, bin_type, bin_code)`）④ §12 新增「**软删后同键重建**」遗留项（TD-1 不做，给三条后续路径：改 UK 含 deleted 须先改基类为 `Long` + 手写软删 / 加「复活」接口 / 改物理删）⑤ 审查记录新增「**裁决记录**」节（C1 已决；C2 / C3 / C4 待定）⑥ 文档头「更新」行补记本轮
- **手法（可复用）**：写 python 脚本做**定点替换**（`.workbuddy/tmp/review-probe/apply_c1.py`，8 处替换 + 命中数校验 + 区间法替换整节）—— 比逐个 Edit 省轮次，且能一次校验「命中数是否符合预期」+ 输出残留计数；**踩点**：§3.4 与 §3.5 的字段说明文本**完全相同**（同文本命中 2 处），校验要允许 2 并按同值替换
- 验证：`startswith('---')` 仍 True；reindex 后 INDEX **无变化**；§3.7 区间拼接后格式完好（表格 + `---` 与 §4 衔接正常）
- **环境踩点（新，重要）**：长链 git 命令（`reindex && git add -A && git commit -q -m … -m …`）**被沙箱拦了一次**（SIGTERM，报 `wmic/reg/sc` 黑名单，显然与本命令无关），但**commit 实际已经成功**、只是输出流被中断 → **判断命令是否成功必须复核 `git log` / `git status`，不要只看 exit code**（我差点误判为提交失败而重试）。后续提交优先用短命令或 `git commit -F <消息文件>`
- 待推：**3 个提交**（789b872 AGENTS 分层 / 64ea476 第 1 轮审查 / 1946a4a C1 落地）
- 仍待定：C2（权限码三级是否收敛为两级）/ C3（V6 判重键是否加 `eqp_id`）/ C4（Bin 字典是否加 `program_version`）；另 **F2** 可顺手把 §5 的乐观锁措辞精确到 MP `@Version` + `updateById` 判影响行数

