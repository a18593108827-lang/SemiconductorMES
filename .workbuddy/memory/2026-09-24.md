# 2026-09-24 工作日志

## C3 落地：V6 判重键加 `eqp_id` + null 分支写法（承接 09-23 的规格审查）
- 用户先问「C3 是要做什么」→ 解释：V6 判重键缺 `eqp_id`，会把「两台测试机并行测同一批」这种**合法的两次测试**误判成重复提交拒掉（`TEST_RECORD_DUPLICATE`），且被拒时无留痕说明原因
- 用户拍板「按『加 eqp_id + null 分支写法』落进规格」
- **落点 6 处**：① §5 V6 行（判重键 → 6 元键 `(lot_id, eqp_id, program_name, program_version, test_time, total_qty)`，标注「两台测试机并行测同批各记一条、互不误判」）② **新增「V6 实现约束」blockquote**（关键：`eqp_id` 属可空字段 → **禁止** `.eq(MesTestRecord::getEqpId, eqpId)`，必须 `.eq(eqpId != null, ...)` / 空值 `.isNull(...)`）③ §9 D12 补判重键与 null 分支说明 ④ §10 并发表**新增场景行**「两台测试机并行测同一批 → 各记一条，不得误判」⑤ 裁决记录 C3 行改已决 ⑥ 文档头补记
- **本轮实证（新踩点，已同步进项目 MEMORY.md）**：MP 的 `addCondition(boolean condition, R column, SqlKeyword, Object val)`（源码 `AbstractWrapper:467-470`）**只判 condition 布尔、不判 val 是否为 null** → `eq(col, null)` 会生成 `col = NULL`（SQL 语义恒 UNKNOWN）→ **判重 / 查询静默失效且不报错**，测试也难发现。可空字段参与条件必须写 `.eq(val != null, col, val)`，null 场景用 `.isNull(col)`。现网判重先例 = `AuthServiceImpl:43-45`（`selectCount` + `AssertUtil.isTrue(count == 0, "……已存在")`）；UK 冲突兜底 = `ComplaintPackageFacadeImpl:560-569`（沿异常链取约束名）
- 验证：脚本 `.workbuddy/tmp/review-probe/apply_c3.py` 5 处定点替换（命中数校验）；`startswith('---')` 仍 True；V6 区域格式复核正常（说明块 + `---` + §6 衔接无误）；reindex 后 INDEX 无变化
- 仍待定：**C4**（Bin 字典是否加 `program_version` 维度）—— 定完 TD-1 规格即收敛完成，可进 plan 批准流程
- 待推提交：承接 09-23 的 6 个 + 本轮

## C4 落地：Bin 字典加 `program_version` 维度 + 四级回退（选项 1）
- 用户先问「没太懂 C4 要干嘛」→ 用例子讲清：字典的「适用范围」只有 `product_code` / `program_name` 两维，而 A9 偏偏要求 `program_version` 不可空（理由 = Bin 语义随版本变）。同一程序 v1 与 v2 若把 Bin3 定义成不同失效模式，字典**表达不出来** → 系统**静默照抄错误的 `bin_name`**，且该错误被 D7 快照**固化进历史与客诉证据包**（等于对客户给错失效模式，车规审厂/8D 场景要命）
- **说服用户的关键洞察**：选 1（加维度）的维护量其实**可控** —— 回退链逐级降级，程序升版但 Bin 语义未变时仍命中 `PROGRAM` / `PRODUCT` / `GLOBAL` 档，**只有版本间真有差异时才需单加一条**。对比选项 2（写成人工保证的假设）的错误后果不对等（静默 + 固化进证据包）
- 用户拍板选项 1
- **落点 10 处**：§3.1 新增 `program_version VARCHAR(32)` 字段行 · `bin_scope` 增至 **4 档**（加 `PROGRAM_VERSION`）· UK 扩为 5 元 `(product_code, program_name, program_version, bin_type, bin_code)` + **空档填 `''` 而非 NULL**（MySQL UK 不收 NULL，与 product_code/program_name 同款处理）· 写入校验（`TEST_BIN_SCOPE_INCONSISTENT`）扩到 4 档 · 「为何独立于 mes_hold_reason」措辞更新 · §5 `GET /test/bins` 查询参数加 `programVersion` · §5 **V2 行改为四级回退**（`PROGRAM_VERSION` → `PROGRAM` → `PRODUCT` → `GLOBAL`）· §9 新增 **D14** · 裁决记录 C4 改已决 · 文档头补记
- 验证：脚本 1 处插行 + 9 处替换（命中数校验）；`startswith('---')` 仍 True；**残留「待定」项 = 0**（C1–C4 全部关闭）
- **TD-1 规格至此收敛完成**：C1–C4 全决 + F1/F2 落地 → 可进 plan 批准流程
- 待推：8 个提交

## TD-1 plan 同步到与规格一致（C1–C4 / F2 / D11–D14）
- 用户拍板「把 plan 同步到与规格一致」
- **改动量**：接口表**整表替换**（12 → 13 行）+ **22 处**定点替换 + 验收**追加 4 条** + 收尾 3 处
- **同步清单**：① 权限码 4 处三级 → 两级，并新增作废端点行 ② 端点 `GET /lots/{id}/test-summary` → `GET /test/summary/by-lot/{lotId}`（Lot 侧不代理）③ **记录号发号方式改为号段表**（原 plan 写「当日 MAX(后缀)+1 + UK 兜底 + 冲突重试」，规格 **D11** 已改为 `mes_test_record_no_seq` + `LAST_INSERT_ID` **两步取号** —— 这条是我上一轮列「5 处不一致」时**漏掉的**，脚本比对时才发现）④ §3 表变更：`mes_bin_def` 补 `program_version` / bin_scope 4 档 / UK 5 元、`migrate_test.sql` 加号段表、两表补 `deleted` TINYINT + UK 不含 deleted、`schema.sql` 5 → 6 张表 ⑤ §5 步骤：b.2 校验改 **V1–V6**、b.3 字典**四级**回退、b.4 含**作废**、b.5 号段表发号 ⑥ 新增 **K11**（软删口径）/ **K12**（判重 null 分支）/ **K13**（乐观锁不手写）⑦ 错误码补 4 个（`TEST_RECORD_DUPLICATE` / `TEST_BIN_SCOPE_INCONSISTENT` / `TEST_BIN_DEF_CONFLICT` / `LOT_STRIP_DUPLICATE`）⑧ 验收 8 改「前置拒绝 `LOT_STRIP_DUPLICATE`」、验收 12 改两级码、**追加验收 16–19**（判重窗口含 null 分支 + 反向验证 / 作废 / 软删口径 / 四级回退）⑨ 并发表补「双机并测各记一条」+ 导出并发口径改 **REPEATABLE READ** 快照读 ⑩ §7 回滚表数 5 → 6 ⑪ M1 补实测值 MAX(id)=332、**M2 标「已定稿，照规格执行」**
- **自查抓到自己 3 处问题并修掉**：typo `exqpId`（应为 `eqpId`）、§1 Test 模块描述漏「号段表 + 作废接口」、验收 8 未同步「前置拒绝」→ 教训：**批量同步后要通读一遍受影响区域**，脚本校验只能保证「替换命中」，保证不了「有没有漏改」
- 验证：替换全部命中数校验；`startswith('---')` 仍 True；残留三级权限码 **0**、残留旧端点 **0**
- **plan 现与规格一致 → 可进批准流程**（plan 仍 `draft`，未批不动码）

## 事故：CI 文档闸门自落地起从未生效 → 修复 + EVAL-0002
- **触发**：用户推送后在 GitHub Actions 截图发现 `docs` job 失败。**红点含义 = 该 commit 的 CI 检查失败**（截图中 `0fffa61` / `9fdf6c8` 两个 commit 带红点；backend / web 两个 job 是绿的）
- **两个独立根因**（都在 `.workbuddy/scripts/add_frontmatter.py`）：
  - **R1 硬编码路径**：原 `DOCS = r"D:\java\xm\2026_07\MES\docs"` → ubuntu runner 无 D 盘；**关键隐蔽点：`os.walk` 对不存在的目录不报错（静默返回空）**，异常直到写 INDEX 才抛（报错在 line 209，病因在 line 6）。修法：`DOCS` 由 `__file__` 上两级推断
  - **R2 生成物非幂等**（修完 R1 才会撞上）：`build_index` 用 `datetime.date.today()` 写 INDEX 的 `updated:` 与「生成：」→ **隔天跑必然产出不同 INDEX → `git diff --exit-code` 假失败**。修法：日期取「内容中的最大 updated」（无内容时回落 today）
- **验证三项（可复现）**：① 本地 md5 前后一致（`c33edeb95d0f…`，行为未变、`git status` 空）② **模拟 runner 目录结构**（脚本 + 2 篇文档复制到陌生路径跑 → 正确产出 INDEX）③ **跨天对照实验**（把副本文档 updated 改 `2026-08-15` → INDEX 的 `updated:` 与「生成：」跟着变，而当时系统日期是 09-24）→ 证明不依赖硬编码路径、也不依赖时钟
- 未执行项：CI 转绿必须推送后在 Actions 确认（**本机无法跑 GitHub Actions，不得据本地通过宣称已修好**）
- **落 EVAL-0002**（`docs/eval/` 第 2 条）：R1/R2 根因 + 修法 + 三项验证证据 + 防复发回流点
- **回流点回填**：`eval/README` 落档口径**新增一档「CI / 工具链自身缺陷」**（原表只覆盖生产事故与验收期缺陷）+ 登记第 2 条；`Doc-4-plan` 补「**验收 6**」记录「当初只校了 `ci.yml` 的 YAML 结构、**未在真实 runner 验证**」这一验收设计缺口，并**更正 F4**（它当时写着「CI 门禁可在下次推送时兜住」，而该门禁**并未生效** → 承诺不成立）
- 提交：`e6220bf`（脚本修复）+ `38e859d`（EVAL-0002 + 回流 + INDEX 106 篇）
- **踩点（新）**：`git commit -m "…D:\java\…"` 里写反斜杠路径 → **bash 把 `\\` 折成正斜杠、引号失衡 → `syntax error`，命令根本没执行**（不是提交失败，是根本没跑）。改用 **`git commit -F <消息文件>`**（消息用 Write 落盘、不经 shell 解析）✔ 与已有踩点「Git Bash heredoc 吃反斜杠」同类

## 闭环确认：CI 已转绿 + 一次「被旧红点误判」的教训
- 用户再次贴截图称「还是有问题」（截图里仍是同样的 `D:\java\...` FileNotFoundError）→ **没有盲信截图，改为直接查 GitHub API 拿真实状态**
- **查询方式（可复用，无需凭据）**：`GET https://api.github.com/repos/a18593108827-lang/SemiconductorMES/actions/runs?per_page=15`（public 仓库匿名可读），返回每个 run 的 `head_sha` / `conclusion` / `created_at` → 可精确判定「哪个 commit 通过/失败」
- **实测结论**：修复前三次 run（`0fffa61` / `9fdf6c8` / `74fc5da`）均 `failure`；**修复后的 run（`head_sha=2ebfbae`，2026-09-24T02:57:33Z）`conclusion=success`** → 三个 job 全过（含 docs 的 `reindex 后 INDEX 必须无差异`）→ **修复已生效**
- **关键教训（易误判）**：GitHub 上**每个 commit 的 check 结果是它自己那次运行的冻结记录** —— 后续 commit 修好变绿，**不会**让旧 commit 的红点消失。用户看到的红点全在修复前的三个 commit 上；判「是否已修好」必须看**最新 run / 最新 commit**，盯历史 commit 的红点会得出错误结论
- 已把该闭环结论与「易误判点」回填入 `EVAL-0002`（原「未执行：待推送后确认」段改为「已确认」）
