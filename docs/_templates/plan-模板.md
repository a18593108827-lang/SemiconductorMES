---
type: plan
module: 
status: draft
slices: [{切片号}]
aligns: []
updated: 
---
# {切片号} 计划 — {名称}

> 对齐：{上游产物 §节} · 状态：draft（**未批不动码**）
> 用法：复制本模板到所属模块目录 `docs/模块/{模块}/`，命名 `{切片号}-plan.md`

## 1. 目标与边界

一句话目标。做什么 / **不做什么**（负面清单必须写）。

## 2. 接口清单

| 方法 | 路径 | 权限码 | 说明 |
|------|------|--------|------|

## 3. 表变更

新表 / 改表 / 种子数据（含表前缀，按模块分）。

## 4. 影响的 Facade 与模块

跨模块只走 Facade；列出将被调用 / 修改的 Facade 方法。

## 5. 实现步骤

子步骤 a / b / c，含顺序依赖与禁止乱序项。

## 6. 测试与验收

- 验收标准（可判真假）：
- 验证方式（命令 / 页面 / SQL）：

## 7. 回滚方式

配置开关 / 数据回退 / 不需要，三选一并说明。

---

**批准记录**：`status` 改为 `approved` 时，在此行写明批准人与日期（该提交即审计轨迹）。
