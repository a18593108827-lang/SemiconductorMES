# docs/intent/ — 意图规格目录

新需求的唯一入口。每个需求一份 `INT-{N}-*.md`（编号四位递增，INT-0001 起），从 `docs/_templates/INT-模板.md` 起稿。

**状态语义**：`draft` → `accepted`（进入规格阶段）/ `rejected`（关闭留档，写拒绝理由）。
**规则**：意图只写问题与验收口径，不写实现方案（P3）；采纳/拒绝以 git 提交为准（A1）。

规范详见 `docs/架构/MES-AI原生SDLC文档体系重构方案.md`。
